[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "envcad"
version = "1.5.10"
description = "跨行业国标工程制图插件（集成 text-to-cad / multiCAD-mcp，覆盖建筑/土木/结构/机械/环保/电气/给排水/暖通/液压/化工及农业食品·电子半导体·能源化工·测绘GIS·桥梁·土壤修复·环境应急·环评等 16+ 行业，国标 DXF 引擎 + COM 桥接）"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "CAD助手" }]
dependencies = [
    "ezdxf>=1.3",
]

classifiers = [
    "Development Status :: 4 - Beta",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Intended Audience :: Engineering",
    "Topic :: Scientific/Engineering :: CAD",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: MacOS",
    "Operating System :: POSIX :: Linux",
]

[project.urls]
Homepage = "https://github.com/kingkemander/cad-helper"
Repository = "https://github.com/kingkemander/cad-helper"
Documentation = "https://github.com/kingkemander/cad-helper/blob/main/USAGE.md"

[project.optional-dependencies]
cad = ["pywin32"]            # multiCAD-mcp COM 桥接（AutoCAD/ZWCAD）
doc = ["openpyxl", "python-docx"]   # 文档自动化层（DOCX 说明书 / XLSX 清单）
step = ["build123d>=0.8"]    # STEP 3D 输出（engine/step_bridge）
all = ["pywin32", "openpyxl", "python-docx", "build123d>=0.8"]

[project.scripts]
envcad = "envcad.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["envcad*"]

[tool.setuptools.package-data]
"*" = ["*.json", "*.yaml", "*.yml", "*.csv", "*.txt"]
